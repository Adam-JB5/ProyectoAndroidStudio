<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PIAndroidStudio">

        <activity
            android:name=".NoticiaActivity"
            android:exported="true"
            android:label="Noticia"
            android:theme="@style/Theme.PIAndroidStudio"/>

        <activity
            android:name=".EquipoActivity"
            android:exported="true"
            android:label="Equipo"
            android:theme="@style/Theme.PIAndroidStudio"/>

        <activity android:name=".EquiposActivity"
            android:exported="true"
            android:label="Equipos"
            android:theme="@style/Theme.PIAndroidStudio"/>

        <activity android:name=".PartidoActivity"
            android:exported="true"
            android:label="Partido"
            android:theme="@style/Theme.PIAndroidStudio"/>

        <activity android:name=".PartidosActivity"
            android:exported="true"
            android:label="Partidos"
            android:theme="@style/Theme.PIAndroidStudio"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PIAndroidStudio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>